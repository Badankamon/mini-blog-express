<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= typeof title !== 'undefined' ? title : 'CleanLife Blog' %></title>
  <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'Expert articles on air quality and water filtration' %>">
  <meta name="keywords" content="blog, articles, air quality, water filtration, health, community">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="author" content="CleanLife Blog">
  <meta name="theme-color" content="#6366f1">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<header>
  <h1><a href="/">✨ CleanLife Blog</a></h1>
</header>

<main>
  <%- body %>
</main>

<footer>
  <p>&copy; 2025 CleanLife Blog. Built with ❤️ for a healthier world. <a href="/" style="color: #6366f1; text-decoration: none;">Back to Home</a></p>
</footer>

<script>
  // Modern page load animation
  document.addEventListener('DOMContentLoaded', function() {
    console.log('✨ CleanLife Blog loaded successfully');
    
    // Add smooth scroll behavior
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  });
</script>

</body>
</html>
